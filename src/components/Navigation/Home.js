import React from "react";

const Home = (props) => {
  return (
    <div>
      <strong>Hello World!</strong>
      <button className="btn btn-primary">button</button>
    </div>
  );
};

export default Home;
